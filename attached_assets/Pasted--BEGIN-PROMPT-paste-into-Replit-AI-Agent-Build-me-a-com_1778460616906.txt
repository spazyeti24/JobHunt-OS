=== BEGIN PROMPT — paste into Replit AI Agent ===

Build me a complete, production-ready "Account Manager Job Hunt Automation" web app on Replit. I am a non-technical user; you must (a) write all the code, (b) install all dependencies, (c) create the file structure, (d) pause at each setup step and give me plain-English instructions for what to do externally (create accounts, copy API keys, share a Google Sheet, etc.), and (e) test the app end-to-end before declaring it done.

LANGUAGE & FRAMEWORK
- Python 3.11+
- Flask for the web dashboard (Jinja2 templates + Bootstrap 5 from CDN for styling — no React, no build step)
- gspread for Google Sheets, google-api-python-client for Google Drive/Docs
- anthropic SDK for Claude
- requests for JSearch and Adzuna
- python-dotenv only as fallback; primary secret storage is Replit Secrets
- Use uv or pip; let Replit auto-install via pyproject.toml or requirements.txt

FILE STRUCTURE TO CREATE
/main.py                  # Flask app entrypoint (the dashboard) — also serves /run-now
/daily_run.py             # Scheduled job entrypoint, runs the full fetch→score→tailor pipeline
/lib/jobs.py              # JSearch + Adzuna fetchers, dedupe
/lib/scoring.py           # Claude Haiku scoring
/lib/tailor.py            # Claude Sonnet tailoring + Google Docs creation
/lib/sheets.py            # gspread wrapper, append_rows, read settings
/lib/drive.py             # Google Drive/Docs helpers
/lib/config.py            # loads env vars, search queries, locations
/templates/index.html     # main dashboard
/templates/settings.html  # threshold + filters
/templates/resume.html    # edit master_resume.md inline
/static/style.css         # optional small overrides
/master_resume.md         # user's source resume (Markdown). Create with a clear placeholder.
/requirements.txt
/.replit                  # entrypoint = python main.py
/README.md                # human-readable setup walkthrough

REPLIT SECRETS (you must prompt me to add each one in the Secrets tab and explain what each is for, in plain English)
- RAPIDAPI_KEY             — for JSearch on RapidAPI
- ADZUNA_APP_ID            — Adzuna application ID
- ADZUNA_APP_KEY           — Adzuna API key
- ANTHROPIC_API_KEY        — Claude
- GOOGLE_SERVICE_ACCOUNT_JSON — paste the entire contents of the service-account JSON file as a single string
- GOOGLE_SHEET_ID          — the ID from the Google Sheet URL
- GOOGLE_DRIVE_FOLDER_ID   — the folder where tailored Docs land
- DASHBOARD_PASSWORD       — simple password gate for the Flask app (use HTTP Basic Auth)

WALK ME THROUGH THESE EXTERNAL SETUP STEPS IN ORDER, ONE AT A TIME, AND WAIT FOR ME TO CONFIRM EACH BEFORE CONTINUING:

1. RapidAPI / JSearch: "Go to rapidapi.com, search 'JSearch by OpenWeb Ninja', click Subscribe → BASIC (free, 200 requests/month — plenty for once-daily use), copy your X-RapidAPI-Key from any code sample, and paste it into Replit Secrets as RAPIDAPI_KEY."
2. Adzuna: "Go to developer.adzuna.com/signup, register, then on the dashboard copy your App ID and App Key. Paste them into Replit Secrets as ADZUNA_APP_ID and ADZUNA_APP_KEY. Adzuna's free tier allows 250 calls/day, 2500/month — more than enough."
3. Anthropic Claude: "Go to console.anthropic.com, click API Keys, create a new key, add a $5 credit balance under Billing (this entire app should cost under $5/month). Paste the key into Replit Secrets as ANTHROPIC_API_KEY."
4. Google Cloud service account (walk me through this carefully, screen by screen):
   a. Go to console.cloud.google.com, create a new project named "Job Hunt Automation".
   b. APIs & Services → Library → enable BOTH "Google Sheets API" and "Google Drive API".
   c. APIs & Services → Credentials → Create Credentials → Service Account. Name it "job-hunt-bot". Skip the optional grants.
   d. Click the new service account → Keys → Add Key → Create new key → JSON. A file downloads.
   e. Open that JSON file in a text editor, copy the ENTIRE contents, paste it into Replit Secrets as GOOGLE_SERVICE_ACCOUNT_JSON.
   f. Note the `client_email` field (looks like job-hunt-bot@…iam.gserviceaccount.com).
5. Google Sheet:
   a. Create a new Google Sheet titled "Job Hunt Tracker".
   b. Click Share, paste the service-account client_email, give it Editor access.
   c. Copy the sheet ID from the URL (the long string between /d/ and /edit) into Replit Secrets as GOOGLE_SHEET_ID.
6. Google Drive folder:
   a. Create a folder in Drive named "Tailored Applications".
   b. Share it with the same service-account email as Editor.
   c. Copy the folder ID from the URL into Replit Secrets as GOOGLE_DRIVE_FOLDER_ID.
7. Set a DASHBOARD_PASSWORD secret of your choice (used to log into the dashboard).

WHEN I CONFIRM ALL SECRETS ARE SET, BUILD THE APP WITH THESE BEHAVIORS:

A) main.py — Flask Dashboard
- HTTP Basic Auth using DASHBOARD_PASSWORD (username is fixed: "me").
- Bootstrap 5 from CDN, dark mode optional.
- Route GET / — main page. Reads from the Google Sheet's "Jobs" tab. Shows a filterable table with columns: date, title, company, location, employment_type, remote, salary, fit_score (colored badge: green ≥80, yellow 60–79, red <60), status, resume link, cover letter link, apply link, notes. Table has:
   - A score-threshold slider (0–100, defaults to current setting), live-filters the table.
   - A job-type multi-select (FULLTIME, PARTTIME, CONTRACTOR, INTERN).
   - A remote-only toggle.
   - A search box that filters by company or title (client-side, JS).
   - Buttons per row: "Mark Applied", "Hide", "Re-tailor" (POST to /retailor/<job_id>).
- Route GET /settings — form to update Settings tab: score_threshold (number), employment_type_filter (checkboxes), remote_only (toggle), daily_search_queries (textarea, one query per line), daily_locations (textarea). POST writes back to the Sheet Settings tab.
- Route GET /resume — textarea pre-filled with current master_resume.md contents. POST overwrites the file. Show a warning that changes apply to future runs only.
- Route POST /run-now — fires daily_run.py in a background thread (use threading.Thread, daemon=True). Returns immediately with a flash message "Run started — check back in 2 minutes."
- Route GET /healthz — returns "ok" (for uptime checks).
- All routes wrapped in @basic_auth decorator.

B) daily_run.py — The Scheduled Job
- Logs every step to stdout with timestamps (Replit captures these in deployment logs).
- Step 1 (Fetch): For each query in Settings.daily_search_queries, call JSearch GET https://jsearch.p.rapidapi.com/search with params: query (combine title + location), date_posted=today, employment_types from Settings, num_pages=2. Headers: X-RapidAPI-Key from secret, X-RapidAPI-Host: jsearch.p.rapidapi.com. Then optionally call Adzuna /v1/api/jobs/us/search/1?app_id=…&app_key=…&what=…&where=… and normalize the response to the same schema. Combine and dedupe on (employer_name + job_title + job_city).
- Step 2 (Skip already-seen): Read existing job_ids from column A of the Jobs tab. Skip any job whose normalized fingerprint is already there.
- Step 3 (Score): For each new job, call Claude Haiku 4.5 with the prompt below. Parse JSON. If parsing fails, set score=0, status="Score failed", rationale=raw text.
- Step 4 (Tailor): For jobs whose fit_score ≥ Settings.score_threshold, call Claude Sonnet 4.5 with the tailoring prompt below. Use the response to create two Google Docs in the Drive folder (one resume, one cover letter), set sharing to "anyone with the link can view", and capture the docs.google.com URLs.
- Step 5 (Write): Append one row to the Jobs tab per job, with columns A–P as documented. Set status: "Tailored" if both Docs were created, "Scored" if above-threshold but Doc creation failed, "Below threshold" otherwise.
- Step 6 (Update last_run_at in Settings).
- Wrap the whole thing in a try/except; on failure write the traceback to a "Run Log" tab.

C) Claude Prompts (put these EXACTLY in /lib/scoring.py and /lib/tailor.py)

SCORING PROMPT (model: claude-haiku-4-5, max_tokens: 800, temperature: 0):
System: "You are a precise hiring fit evaluator for Account Manager roles. You return ONLY valid JSON, no prose, no markdown fences."
User: "Evaluate fit between this candidate and this job.

CANDIDATE RESUME (Markdown):
<<<
{master_resume_markdown}
>>>

JOB POSTING:
Title: {job_title}
Company: {company}
Location: {location} (remote: {is_remote})
Employment type: {employment_type}
Description:
<<<
{job_description}
>>>

Return JSON exactly in this shape:
{
  \"score\": <integer 0-100>,
  \"score_rationale\": \"<2-3 sentences, specific to this candidate and this job>\",
  \"must_have_matches\": [\"<short bullet>\", ...],
  \"gaps\": [\"<short bullet>\", ...],
  \"red_flags\": [\"<e.g. requires security clearance / on-site only / unrealistic experience>\", ...]
}

Scoring rubric:
- 90-100: near-perfect match, candidate exceeds requirements
- 75-89: strong match, minor gaps
- 60-74: decent match, several gaps but worth applying
- 40-59: weak match, would be a stretch
- 0-39: poor fit, do not apply
Be honest. Do not inflate scores."

TAILORING PROMPT (model: claude-sonnet-4-5, max_tokens: 4000, temperature: 0.4):
System: "You are an expert career coach who tailors Account Manager resumes and cover letters. You never invent achievements, metrics, employers, or dates. You preserve all factual content from the source resume and only re-order, re-phrase, and re-emphasize. You write in the candidate's own voice based on their resume. Output ONLY valid JSON, no prose, no markdown fences."
User: "Tailor this candidate's resume and write a cover letter for this specific job.

CANDIDATE MASTER RESUME (Markdown — the source of truth, do not invent anything not in here):
<<<
{master_resume_markdown}
>>>

TARGET JOB:
Title: {job_title}
Company: {company}
Description:
<<<
{job_description}
>>>

Return JSON exactly in this shape:
{
  \"tailored_resume_markdown\": \"<the full tailored resume in Markdown. Re-order bullets to put the most job-relevant ones first under each role. Swap generic verbs for stronger ones. Use language from the job description WHERE TRUE. Keep all dates, employers, and metrics exactly as in the source. Maximum 1 page equivalent.>\",
  \"cover_letter_markdown\": \"<a 250-word cover letter addressed to 'Hiring Team at {company}'. Three paragraphs: (1) opening with specific role and one sentence on why this company; (2) two concrete bullet-style sentences mapping the candidate's experience to the top job requirements; (3) closing with availability and a specific ask. Professional but warm. No clichés ('I am writing to apply…'). No invented details.>\",
  \"top_keywords_used\": [\"<keyword>\", ...],
  \"summary_of_changes\": \"<2 sentences describing what was emphasized vs. the master resume>\"
}"

D) Google Docs creation (in /lib/drive.py)
- Use the google-api-python-client. Build the Drive service with the service-account creds.
- For each Doc: create with mimeType='application/vnd.google-apps.document' in parents=[GOOGLE_DRIVE_FOLDER_ID], then use the Docs API batchUpdate to insertText at index 1 with the Markdown converted to plain text (do a light Markdown→plain-text pass; headings become bold lines, bullets become "• "). Set anyone-with-link viewer permission. Return webViewLink.

E) Sheet bootstrap
- On first run, if the "Jobs" tab doesn't exist, create it with the header row A–P listed in the spec. Create a "Settings" tab with defaults: score_threshold=70, employment_type_filter=FULLTIME,CONTRACTOR, remote_only=FALSE, daily_search_queries=["Account Manager","Senior Account Manager","Strategic Account Manager","Customer Success Manager"], daily_locations=["Austin, TX","Remote, US"], last_run_at=blank. Create a "Run Log" tab for errors.

F) Replit Scheduled Deployment setup — at the end, walk me through:
"Click Deploy in the header. Pick 'Scheduled'. Build command: pip install -r requirements.txt. Run command: python daily_run.py. Schedule description: 'Every day at 8:00 AM US/Central'. Timeout: 10 minutes. Click 'Add deployment secret' for every Secret in your workspace (Replit will copy them automatically — confirm). Click Deploy."
Then walk me through creating a separate Autoscale deployment for main.py for the dashboard, with run command: gunicorn -w 2 -b 0.0.0.0:$PORT main:app.

G) README.md should include:
- Plain-English description
- One-paragraph "what it does daily"
- Cost estimate ($1–5/month)
- "How to add a new search query" → edit /settings on the dashboard
- "How to update my resume" → edit /resume on the dashboard
- Troubleshooting: "If no jobs appear, check the Run Log tab in the sheet"

H) FINAL STEP — Test end-to-end:
- Run main.py locally in the Repl, open the webview, log in, click "Run Now".
- Watch the logs in the Replit console. Confirm at least 1 row appears in the Google Sheet.
- Open a tailored Google Doc from the Sheet link to confirm it rendered.
- Only after this works, prompt me to deploy.

CONSTRAINTS
- Never hard-code any API key in source code. Always read from os.environ.
- Never put more than 500 chars of job description in column J (truncate with ellipsis); the full description is implicit from the job_url.
- All Sheet writes use append_rows or batch_update — never one cell at a time.
- All Claude calls use temperature=0 for scoring and 0.4 for tailoring, and always pass max_tokens explicitly.
- Wrap every external API call in a retry-with-exponential-backoff (3 attempts).
- Log to stdout, not files (Replit captures stdout in deployment logs).
- Do not use Streamlit, Django, React, or any build step. Flask + Jinja + CDN-Bootstrap only.

Now: start by confirming you understand the spec, list the dependencies you'll add to requirements.txt, and then ask me to begin Step 1 of the external setup (RapidAPI / JSearch key). Walk me through each setup step one at a time, pausing for my "done" before continuing.

=== END PROMPT ===